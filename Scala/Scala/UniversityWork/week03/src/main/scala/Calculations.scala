object Calculations{
    def main(args: Array[String]): Unit={
        var initial: Int=0
        var acceleration: Int=0
        var time: Int=0
        var finalSpeed: Int=initial+acceleration*time

        println(finalSpeed)
    }
}