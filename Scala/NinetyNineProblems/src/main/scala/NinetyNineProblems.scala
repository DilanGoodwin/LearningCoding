
object mainClass{
  def main(args:Array[String]): Unit={
    //For Testing Purposes Only
    println("It Worked")
  }
}

object Lists{
  
  //P01 - Find the last element of a list
  def getLast(numbs:List[Int]): Int={
    return 0
  }

  //P02 - Find the last but one element of a list
  def getLastButOne(numbs:List[Int]): Int={
    return 0
  }

  //P03 - Find the Kth element of a list
  def findKthElement(numb:List[Int]): Int={
    return 0
  }

  //P04 - Find the number of elements of a list
  def findNumbElements(numb:List[Int]): Int={
    return 0
  }

  //P05 - Reverse a list
  def getReversedList(numb:List[Int]): List[Int]={
    return numb
  }

  //P06 - Find out whether a list is a palindrome
  def listPalindrome(numb:List[Int]): Boolean={
    return false
  }

  //P07 - Flatten a nested list structure
  def flattenList(numb:List[Int]): List[Int]={
    return numb
  }

  //P08 - Remove consecutive duplicates within list elements
  def removeConsecutives(numb:List[Int]): List[Int]={
    return numb
  }

  //P09 - Move consecutives within a list into sublists within a list
  def moveConsecutiveSublists(numb:List[Int]): List[Int]={
    return numb
  }

  //P10 - Run length encoding list
  def runLengthEncoding(numb:List[Int]): List[Int]={
    return numb
  }

  //P11 - RLE with elements no duplicates copied directly result list
  def modifiedRunLengthEncoding(numb:List[Int]): List[Int]={
    return numb
  }
}
