
object mainClass{
  def main(args:Array[String]): Unit={
    println("It Worked")
  }
}

object Lists{
  
  //Find the last element of a list
  def getLast(numbs:List[Int]): Int={
    return 0
  }

  //Find the last but one element of a list
  def getLastButOne(numbs:List[Int]): Int={
    return 0
  }

  //Find the Kth element of a list
  def findKthElement(numb:List[Int]): Int={
    return 0
  }

  //Find the number of elements of a list
  def findNumbElements(numb:List[Int]): Int={
    return 0
  }

  //Reverse a list
  def getReversedList(numb:List[Int]): List[Int]={
    return numb
  }

  //Find out whether a list is a palindrome
  def listPalindrome(numb:List[Int]): Boolean={
    return false
  }

  //Flatten a nested list structure
  def flattenList(numb:List[Int]): List[Int]={
    return numb
  }

  //Remove consecutive duplicates within list elements
  def removeConsecutives(numb:List[Int]): List[Int]={
    return numb
  }

  //Move consecutives within a list into sublists within a list
  def moveConsecutiveSublists(numb:List[Int]): List[Int]={
    return numb
  }

  //Run length encoding list
  def runLengthEncoding(numb:List[Int]): List[Int]={
    return numb
  }
}
